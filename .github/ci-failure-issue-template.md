## CI 流水线失败

| 项目 | 详情 |
|------|------|
| **分支** | `${BRANCH}` |
| **提交** | `${COMMIT_SHORT}` — ${COMMIT_MSG} |
| **触发者** | @${ACTOR} |
| **运行日志** | [查看详情](${RUN_URL}) |

### 失败的 Job

${FAILED_JOBS}

### 失败日志摘要

${FAILURE_LOGS}

> 🤖 此 Issue 由 CI 流水线自动创建。以上为自动提取的关键错误日志片段，完整日志请点击上方 **查看详情** 链接。